# Grouper instance configuration (container level only)
GROUPER_BASE_URL=https://localhost:9443/grouper-ws/servicesRest/json/v4_0_000
GROUPER_DEBUG=true
NODE_TLS_REJECT_UNAUTHORIZED=0

# Authentication credentials are provided by AI agents via OAuth Bearer tokens
# No credentials are stored in the container