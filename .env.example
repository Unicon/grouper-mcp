# Grouper MCP Server Configuration Example
# Copy this file to .env and update with your values

# ============================================================================
# Grouper Web Services Configuration (Required)
# ============================================================================

# Base URL for Grouper web services API (v4)
# Example: https://grouper.example.edu/grouper-ws/servicesRest/json/v4_0_000
GROUPER_BASE_URL=https://your-grouper-instance.edu/grouper-ws/servicesRest/json/v4_0_000

# Basic authentication credentials for Grouper API
GROUPER_USERNAME=your_username
GROUPER_PASSWORD=your_password

# ============================================================================
# Act As Subject (Optional)
# ============================================================================
# If specified, operations will be performed as this subject
# Useful for administrative operations or delegated access

# Subject ID to act as (e.g., 12345678)
#GROUPER_ACT_AS_SUBJECT_ID=

# Subject source ID (e.g., jdbc, ldap)
#GROUPER_ACT_AS_SUBJECT_SOURCE_ID=

# Subject identifier (e.g., username)
#GROUPER_ACT_AS_SUBJECT_IDENTIFIER=

# ============================================================================
# HTTPS Server Configuration (Optional - for remote access)
# ============================================================================

# Port for HTTPS server (default: 3000)
PORT=3000

# Enable/disable HTTPS (default: true)
# Set to 'false' to run in HTTP mode (not recommended for production)
USE_HTTPS=true

# Custom SSL certificate paths (optional)
# If not set, defaults to certs/cert.pem and certs/key.pem
#SSL_CERT_PATH=/path/to/custom/cert.pem
#SSL_KEY_PATH=/path/to/custom/key.pem

# ============================================================================
# Logging Configuration (Optional)
# ============================================================================

# Directory for log files (default: ~/.grouper-mcp/logs/)
#GROUPER_LOG_DIR=/custom/log/directory

# Enable verbose debug logging (default: false)
# Set to 'true' to log detailed request/response information
GROUPER_DEBUG=false

# ============================================================================
# Notes
# ============================================================================
#
# - This file is for documentation purposes. Copy to .env for actual use.
# - Never commit .env files to version control
# - All paths should be absolute, not relative
# - For stdio mode (Claude Desktop), only Grouper configuration is needed
# - For HTTPS mode, configure server settings as well
