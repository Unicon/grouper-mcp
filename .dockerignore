# Node modules
node_modules/
npm-debug.log*

# Build output (will be generated during build)
dist/

# Development files
*.md
docs/

# Git
.git/
.gitignore

# Environment files (should be provided at runtime)
.env
.env.*

# Config files - only copy if explicitly created (not the .example)
config/grouper-mcp.properties.example

# Claude Desktop config (contains credentials)
claude_desktop_config.json

# Logs (will be generated at runtime)
*.log
logs/

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Docker files (not needed inside the image)
Dockerfile
Dockerfile.http
.dockerignore
docker-compose.yml

# Certs directory (should be mounted if needed)
certs/
